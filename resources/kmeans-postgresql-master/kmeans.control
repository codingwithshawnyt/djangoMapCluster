# kmean extension
comment = 'k-means classification by window function'  # Description of the extension
default_version = '1.1.0'                              # Specifies the default version of the extension
module_pathname = '$libdir/kmeans'                     # Path to the dynamic library containing the extension's code
relocatable = true                                     # Indicates whether the extension can be installed in any schema
