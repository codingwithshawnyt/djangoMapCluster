-- Drop the kmeans function that takes an array of floats and an integer as parameters
DROP FUNCTION kmeans(float[], int);
-- Drop the kmeans function that takes an array of floats, an integer, and another array of floats as parameters
DROP FUNCTION kmeans(float[], int, float[]);
